#!/bin/zsh

# Brewfile hash: {{ include "dot_Brewfile" | sha256sum }}

if [[ ! -e "~/.Brewfile" ]]; then
	chezmoi execute-template < $(chezmoi source-path)/dot_Brewfile.tmpl > ~/.Brewfile
fi

brew bundle --global --no-lock
