text/plain; more

text/html; open %s && sleep 2; needsterminal;
text/html; w3m -I %{charset} -T text/html -dump %s; copiousoutput;

image/*; open %s && sleep 2; needsterminal;
audio/*; open %s && sleep 2; needsterminal;

application/msword; open %s && sleep 2; needsterminal;
application/pdf; open %s && sleep 2; needsterminal;
application/postscript; open %s && sleep 2; needsterminal;
