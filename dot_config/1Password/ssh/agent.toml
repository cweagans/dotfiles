account = "Eagans"

[[ssh-keys]]
item = "SSH Key"
vault = "Private"

[[ssh-keys]]
item = "util.cweagans.net ssh"
vault = "Network"

# Generally, this key shouldn't be used. It can be available in the agent if needed though.
#[[ssh-keys]]
#item = "Old SSH Key (RSA)"
#vault = "Private"
