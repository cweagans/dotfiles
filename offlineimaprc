[general]
ui = ttyui
accounts = Personal,NBC
pythonfile = ~/.dotfiles/offlineimap.py
fsync = False

[Account Personal]
localrepository = Personal-Local
remoterepository = Personal-Remote
status_backend = sqlite

[Repository Personal-Local]
type = Maildir
localfolders = ~/Mail/Personal

[Repository Personal-Remote]
type = IMAP
remotehost = mail.messagingengine.com
remoteport = 993
ssl = yes
cert_fingerprint = 958b1601563aef92607a41eb5bad22e3cace8431
holdconnectionopen = yes
remoteuser = cweagans@fastmail.com
remotepasseval = get_keychain_pass(account="cweagans@fastmail.com", server="mail.messagingengine.com")
folderfilter = lambda folder: folder not in ['INBOX.Trash', 'INBOX.Junk Mail']

[Account NBC]
localrepository = NBC-Local
remoterepository = NBC-Remote
status_backend = sqlite

[Repository NBC-Local]
type = Maildir
localfolders = ~/Mail/NBC

[Repository NBC-Remote]
type = IMAP
remotehost = localhost
remoteport = 1143
ssl = no
holdconnectionopen = yes
remoteuser = tfayd\206451874
remotepasseval = get_keychain_pass(account="cameron.eagans@nbcuni.com", server="ecmail.inbcu.com")
folderfilter = lambda folder: folder not in ['Calendar', 'Contacts', 'Deleted Items', 'Deleted Messages', 'Journal', 'Junk E-Mail', 'Notes', 'Outbox', 'Tasks']
